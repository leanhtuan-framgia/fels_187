<% provide :title, t("header.users") %>
<h1><%= t "header.users" %></h1>

<div class="row">
  <div>
    <%= link_to t("btn.create_user"), new_admin_user_path, class: "btn btn-primary" %>
  </div>

  <div class="search center">
    <%= render "admin/shared/search_form", name: Settings.search_field.user %>
  </div>
  <br>
  <div class="row users">
    <%= render @users if @users.any? %>
  </div>
</div>
