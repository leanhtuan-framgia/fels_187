<%= form_for @relationship, remote: true do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id, id: "followed_id" %></div>
  <%= f.submit t("btn.follow"), id: "submit",
    class: "btn btn-primary form-control" %>
<% end %>
